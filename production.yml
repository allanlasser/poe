version: '2'
services:
    nginx:
        image: jwilder/nginx-proxy
        container_name: nginx-proxy
        ports:
            - "80:80"
        volumes:
            - /var/run/docker.sock:/tmp/docker.sock:ro
    feathers:
        build:
            context: .
            dockerfile: Dockerfile.feathers.production
        expose:
            - "3030"
        depends_on:
            - postgres
            - redis
        environment:
            - PORT=3030
            - VIRTUAL_HOST=159.203.93.123
    redis:
        image: redis
        expose:
            - "6379"
    postgres:
        image: postgres
        expose:
            - "5432"
        environment:
            - POSTGRES_USER=massive
            - POSTGRES_PASSWORD=science
            - POSTGRES_DB=massivedb
