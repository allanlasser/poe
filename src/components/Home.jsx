import React from 'react';
import { Link } from 'react-router';

import TitleList from '../containers/TitleList';

const Home = () => (
  <div id="home">
    <TitleList />
  </div>
);

export default Home;
